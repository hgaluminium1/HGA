import { defineCloudflareConfig } from "@opennextjs/cloudflare";

/**
 * Phase 0 stub — OpenNext Cloudflare adapter config.
 * Real caching / R2 / KV bindings land with dual-host hardening (Phase 10).
 */
export default defineCloudflareConfig({});
