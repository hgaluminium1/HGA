export default function AdminDashboardPage() {
  return (
    <main className="mx-auto max-w-3xl p-8">
      <h1 className="text-2xl font-semibold">Admin — Phase 0</h1>
      <p className="mt-2 text-[var(--color-text-muted,#64748b)]">
        Auth guard and CMS shell come in later phases. Placeholder only.
      </p>
    </main>
  );
}
